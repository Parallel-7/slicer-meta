name: Build and Publish Package

on:
  # Tags only (matching ff-5mp-api-ts): a main push used to publish a
  # timestamped prerelease on every merge, accumulating junk versions in the
  # registry. Cutting a release is exactly one action: push a `v*` tag.
  # workflow_dispatch stays for deliberate off-tag prerelease builds.
  push:
    tags: [ 'v*' ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write # npm trusted publishing (OIDC)

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    environment: release # must match the npm trusted-publisher registration
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@parallel-7'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Set version
        id: set_version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            # If this is a tag push, use the tag version
            TAG=${GITHUB_REF#refs/tags/}
            VERSION=${TAG#v}
          else
            # If this is not a tag push, use the current version and add timestamp
            CURRENT_VERSION=$(node -p "require('./package.json').version")
            TIMESTAMP=$(date +%Y%m%d%H%M%S)
            VERSION="${CURRENT_VERSION}-${TIMESTAMP}"
          fi
          echo "Using version: $VERSION"
          npm version $VERSION --no-git-tag-version --allow-same-version
          echo "version=$VERSION" >> $GITHUB_OUTPUT
      
      - name: Publish to GitHub Packages
        run: npm publish --registry=https://npm.pkg.github.com
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upgrade npm for trusted publishing (OIDC needs >= 11.5.1)
        run: npm install -g npm@latest

      - name: Publish to npm (public, trusted publishing)
        run: npm publish --registry=https://registry.npmjs.org/
          
      - name: Report error details if failed
        if: failure()
        run: |
          echo "::error::Package publish failed. Please check authentication and configuration."
          echo "::group::NPM Config"
          npm config list
          echo "::endgroup::"
          echo "::group::Repository Info"
          echo "Owner: ${{ github.repository_owner }}"
          echo "Repo: ${{ github.repository }}"
          echo "::endgroup::"
